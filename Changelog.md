## v2.5.41

### 更新源可靠性

- Windows/Linux 点击更新继续优先使用神仙云 Dufs 元数据与安装包；Dufs 下载或校验失败时，自动切换到神仙云 GitHub Release 备用源并在应用内完成签名校验与安装。
- 备用源失败、版本不一致或签名校验失败时明确提示并保留原安装，不再静默打开 GitHub 网页。
- 设置页发布链接统一指向神仙云自己的 Release，避免误跳上游项目。

## v2.5.40

### 商业化稳定性与隐私

- 设备识别统一使用本机稳定派生键，原始系统标识不离开设备；覆盖安装和重装后继续识别为同一设备。
- 流量累计与上报支持单请求在途、持久化续报、失败退避和服务端限流，网络恢复后自动补报且避免重复计费。
- 受管订阅统一显示为“神仙云 官方订阅”，托盘、通知、服务提示和多语言界面不再暴露上游名称、提取码或真实订阅信息。
- 提取码导入、旧版一键导入和受管订阅刷新继续保持事务回滚；凭据异常时按失败关闭处理并隐藏敏感配置名称。
- GitHub 正式发布页、下载链接和更新清单增加神仙云仓库隔离校验，NAS 下载入口仍由校验后的发布资产原子切换。

## v2.5.39

### 提取码导入可靠性

- 首次导入不再因为所有节点暂时离线或延迟检测失败而拒绝写入，离线环境也可以先保存有效订阅。
- 继续校验订阅格式并以核心实际启动作为最终验证；配置无效或核心启动失败时仍会恢复导入前状态。

## v2.5.38

### 稳定性与性能

- 流量累计上报改为五分钟基础间隔并加入随机抖动，避免大量客户端同时请求服务器。
- 同一客户端只允许一个流量请求在途；失败时指数退避并遵循服务端 `Retry-After`。
- 取消界面生命周期清理时的额外上报，降低网络波动和程序退出时的重复请求。
- 保留本地持久化累计计数，断网、服务繁忙或程序异常退出后可以安全续报，不重复计费。

## v2.5.34

### 提取码导入可靠性

- 导入新提取码时先检测主策略组节点；默认节点失效但仍有其它可用节点时，自动选择延迟最低的健康节点。
- 新节点通过真实代理链路检查后才提交提取码和配置；全部节点不可用时恢复原订阅、原提取码、核心与网络状态。
- 节点预检会排除直连、拒绝、兼容占位、重复及超时节点，避免把特殊策略误当作可用代理。

## v2.5.33

### 显示修复

- 修复系统深色模式下首页节点选择菜单文字与延迟几乎不可见的问题。
- 节点菜单补齐选中、悬停和键盘焦点状态，浅色玻璃弹窗文字不再错误继承深色主题颜色。

## v2.5.32

### 核心可靠性

- 全面加固提取码订阅的导入、切换和更新事务；失败时恢复原活动配置、规则与网络状态，避免留下半成品配置。
- 加固核心单实例、启动就绪、停止超时和服务/Sidecar 状态判断；旧核心清理失败时不再误报已停止。
- “彻底重置”增加分阶段清理与回滚，核心、系统服务或网络清理失败时中止重建并保留可诊断状态，避免重复管理员授权。
- 改善覆盖安装、同目录卸载重装后的旧服务、旧进程、任务计划与配置残留诊断。

### 使用体验

- 切换提取码后，Web 管理页面绑定当前活动配置和控制器，默认打开实时连接页。
- 服务器检测在官方主线路直连失败后允许经当前内核或后台代理复测，修复浏览器可访问但客户端显示“未连通”的误报。
- 扩充自检信息，展示关键配置目录、核心 PID、系统服务与计划任务状态，方便定位升级和重置问题。

## v2.5.31

### 修复问题

- 修复旧版覆盖安装、同目录卸载重装后核心无法启动，换安装目录才恢复的问题。
- 安装版检测到旧便携版 `.config/PORTABLE` 残留时自动禁用该标记，并保留旧目录数据用于恢复。
- Windows 覆盖安装前可靠停止并删除旧服务，必要时结束损坏的服务进程；拒绝覆盖仍被锁定的旧核心文件，避免新旧文件混装。
- 服务模式启动核心失败时自动关闭 TUN 并降级到 Sidecar，保证普通代理仍能启动，同时保留明确错误日志。
- 切换提取码或活动配置后，Web 管理页面会读取当前控制器和配置，不再显示切换前的旧节点。
- 默认 Web 管理入口改为实时连接页，可查看目标域名/IP、命中规则和实际代理链。
- 配置切换时刷新控制器缓存，并隔离不同配置的浏览器面板会话。

## v2.5.29

### 正式版

- “彻底重置”增加删除旧配置并生成全新配置的选项，解决旧配置与新内核协议不兼容时无法启用的问题。
- 重置前可自动创建本地恢复备份，并可同时修复系统服务。
- 重置时保留日志、备份和稳定设备标识，避免同一台设备重复占用设备名额。
- 支持短时一次性 Ticket、受管订阅、设备凭据、在线心跳和幂等流量上报。
- 同时保留旧订阅 URL 和旧网页一键导入协议，兼容尚未升级的客户端。
- 网站全保护继续保持关闭，兼容仍使用旧订阅方式的客户端。

## v2.5.29-rc.2

### 预览功能

- 提取码和网页一键导入改用短时一次性 ticket，客户端界面与本地配置不再保存或显示真实订阅链接
- 导入后的订阅名称和文件名统一显示当前提取码，避免把后台文件名误认为提取码
- 设备令牌、受管订阅地址和授权信息加密保存在应用私有目录
- 增加 v2 在线心跳、离线通知和幂等流量上报，设备或流量超限时停止代理
- 受管配置允许本地编辑；保存后自动转为本地副本，手动更新时可明确恢复远程管理
- 保留旧订阅链接导入兼容，vpn-web 严格保护开关将在预览真机验证通过后再启用

## v2.5.26

### 修复问题

- 修复客户端无法访问 `api.sxnn.de:5443`，导致提取码、订阅、心跳和流量上报失败
- 提取码导入增加检查、获取订阅、启动联网的可视化流程与失败恢复操作
- 隐藏心跳和订阅请求的底层错误，只显示简明处理提示
- 服务器线路完全自动选择，高级设置只显示汇总连通状态
- 国内主域名固定直连，直连失败时自动切换到可用服务器
- 全局模式改用 `GLOBAL` 节点列表，节点选择会直接更新全局出口
- 同步 Mihomo 插件 0.5.4，改善日志连接与小写配置枚举兼容性

## v2.5.2

### 🐞 修复问题

- macOS 托盘速率可能的样式错误
- 修复订阅 TLS 1.0/1.1 等过旧协议时显示更明确错误原因
- 修复 gzip 压缩订阅响应被当作无效 YAML 导致导入失败的问题
- 修复订阅 URL 使用空密码 Basic Auth 时未发送认证信息的问题
- Linux 托盘可能与其他 tauri 程序托盘冲突导致图标异常
- 修复前端连接页面导致的内存泄漏
- macOS 12(Monterey) 首页 IP 卡兼容性

<details>
<summary><strong> ✨ 新增功能 </strong></summary>

- 增加 TrustTunnel, OpenVPN, Tailscale, GostRelay 节点显示支持

</details>

<details>
<summary><strong> 🚀 优化改进 </strong></summary>

- 关闭 autofill 弹出窗口

</details>
